#!/bin/bash
APP_PATH="$2/AppleBlox.app"
xattr -cr "$APP_PATH"
LOGGED_IN_USER=$(stat -f "%Su" /dev/console)
if [ -n "$LOGGED_IN_USER" ] && [ "$LOGGED_IN_USER" != "root" ]; then
    chown -R "$LOGGED_IN_USER" "$APP_PATH"
fi
exit 0
